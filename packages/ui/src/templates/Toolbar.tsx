import { styled } from '@mui/material/styles';
// Hàng tiêu đề/hành động phía trên nội dung.
export const Toolbar = styled('div')({
  display: 'flex',
  alignItems: 'center',
  gap: 12,
  marginBottom: 16,
});
